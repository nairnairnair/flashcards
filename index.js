// This is where your project starts.
const Game = require('../src/Game');
const prototypeData = require('../src/prototypeData')

const game = new Game(prototypeData)
 
console.log('Your project is running...'); 
